
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side reads and writes by default.
    // Server-side operations using Admin SDK will bypass these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
